import { createSlice } from "@reduxjs/toolkit";


const initialState = {
    value: 0,
}
const dicentSlice = createSlice({
    name: 'dicent',
    initialState,
    reducers: {

    }
})

export default dicentSlice.reducer